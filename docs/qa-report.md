# Integrated QA report — 2026-09-14

The final integrated `npm run qa` completed successfully after the last code changes: 30/30 automated tests passed; TypeScript checking passed; all English locale files passed key validation (M1.1 89, M1.2 64, M2.2 120, M3.2 137 keys); all four production builds succeeded; and four newly generated SCORM ZIPs passed structural and local HTTP serve validation. The development hub and each game's HTML and module entry returned HTTP 200 from the local Vite server. The M1.2 build emits Vite's large-chunk advisory (about 767 KB uncompressed JavaScript, 207 KB total ZIP), with no build failure.

| Area | Automated evidence | Manual browser evidence / remaining limit |
| --- | --- | --- |
| M1.1 Mission Loadout | Six rule tests cover valid alternatives, incompatible selection, readiness blocking, targeted correction, score and 80-point threshold. Build and ZIP pass. | An agent exercised first launch, mission A equipment selection, an unresolved propeller hold, targeted correction, 32-point clear, advancement to mission B, keyboard focus, and reload/resume. Remaining missions, final result, replay, narrow-window layout and direct LMS run were not manually exercised. |
| M1.2 Site Mission Control | Three rule tests cover safe/unsafe plan, known/new exclusion detection, scoring, mandatory response and landing radius. Build and ZIP pass. | A browser launch attempt exposed a development hub script-path issue; the path was corrected and HTTP script serving checked. The 3D flight, hover, contingency, return, performance, keyboard controls, replay and console were **not manually verified** after the correction. |
| M2.2 Thermal Triage | Six rule tests cover all four observations, environmental artefact, moisture validation, prioritisation, retries and checkpoint restore. Build and ZIP with eight SVGs pass. | No integrated browser interaction pass was completed. Visual readability, keyboard flow, result/replay and console remain manually unverified. |
| M3.2 Digital Handover | Seven rule tests cover spatial mismatch, alignment, discrepancy, blocked outdated/draft/failed-QA releases, approved release, score and checkpoint restore. Build and ZIP pass. | No integrated browser interaction pass was completed. Visual overlay interaction, keyboard flow, result/replay and console remain manually unverified. |
| Shared SCORM | Mock tests cover initialization, incomplete/failed/passed status, 0–100 score policy, best passing score on replay, session time, suspend data, commit, termination, and fallback after failed LMS init. | Per-game Open edX/XBlock launches and actual LMS persistence were not available for manual verification. The mock inspector is in the hub. |
| Localisation | English references are non-empty and consistent with any additional locale files; translator English and missing-key fallback are tested. Source inspection found no unsupported regulatory/AI teaching text in production apps. | Translated-language expansion could not be checked because no partner translations were supplied. |
| Packaging | ZIP validator confirms well-formed root manifest, one SCO launch, referenced assets, no `node_modules` or absolute local paths/secrets, and successful local HTTP serving of launch and generated assets. | Actual Open edX upload/import was not performed. |

The computer-use browser session was stopped with Escape during QA. No further UI automation was attempted. For that reason, this report does **not** claim the full manual QA matrix or production acceptance is complete. The most important remaining acceptance pass is M1.2's full plan → direct 3D flight → changed condition → safe return → landing route in a normal desktop browser, followed by complete success/mistake/replay/resume passes for M2.2 and M3.2 and an Open edX SCORM import check for all four.
